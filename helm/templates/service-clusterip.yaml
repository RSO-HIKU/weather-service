apiVersion: v1
kind: Service
metadata:
  name: {{ include "weather-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "weather-service.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "weather-service.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.clusterIP.port }}
      targetPort: {{ .Values.service.clusterIP.port }}
