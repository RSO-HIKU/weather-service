apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "weather-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "weather-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "weather-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "weather-service.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.clusterIP.port }}
          {{- if .Values.azure.enabled }}
          command:
            - sh
            - -c
            - |
              set -euo pipefail
              
              # JWT Auth (Keycloak / OIDC)
              export KUMULUZEE_JWT_AUTH_ISSUER="{{ .Values.jwtAuth.issuer }}"
              export KUMULUZEE_JWT_AUTH_JWKS_URI="{{ .Values.jwtAuth.jwksUri }}"

              # Run the Java application
              exec java -jar /app/weather-service.jar
          {{- end }}
          env:
            - name: KUMULUZEE_ENV_NAME
              value: {{ .Values.environment | default "dev" | quote }}
            {{- if .Values.database.enabled }}

            # JWT Auth (Keycloak / OIDC)
            - name: KUMULUZEE_JWT_AUTH_ISSUER
              value: {{ .Values.jwtAuth.issuer | quote }}
            - name: KUMULUZEE_JWT_AUTH_JWKS_URI
              value: {{ .Values.jwtAuth.jwksUri | quote }}
            {{- end }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}